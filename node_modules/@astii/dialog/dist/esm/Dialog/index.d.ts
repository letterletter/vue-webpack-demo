import React from "react";
type CalStyle = number | string;
export interface DialogProps {
    title?: React.ReactNode | string;
    width?: CalStyle;
    open?: boolean;
    children?: React.ReactNode;
    footer?: React.ReactNode;
    inside?: boolean;
    draggable?: boolean;
    maskClosable?: boolean;
    contentClassName?: string;
    onClose?: () => void;
}
declare const Dialog: {
    ({ open, title, width, children, footer, draggable, inside, maskClosable, contentClassName, onClose, }: DialogProps): React.JSX.Element | null;
    Icon: ({ name, className, style, onClick, }: {
        name: "close" | "plus" | "format" | "preview" | "up" | "down";
        className?: string | undefined;
        style?: React.CSSProperties | undefined;
        onClick?: (() => void) | undefined;
    }) => React.JSX.Element;
};
export default Dialog;
