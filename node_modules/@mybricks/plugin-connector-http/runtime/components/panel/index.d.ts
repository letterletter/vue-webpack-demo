import React, { CSSProperties, ReactNode } from 'react';
export interface PanelWrapProps {
    style?: CSSProperties;
    className?: string;
    title?: string;
    extra?: ReactNode;
    onClose?(): void;
    children?: ReactNode;
}
export interface PanelWrapRef {
    registerBlur(key: string, blur: () => void): void;
}
declare const _default: React.ForwardRefExoticComponent<PanelWrapProps & React.RefAttributes<PanelWrapRef>>;
export default _default;
