import { FC } from 'react';
type ValueType = any;
export interface RadioButtonsProps {
    options: Array<{
        title: string;
        value: ValueType;
    }>;
    defaultValue: ValueType;
    onChange?(value: ValueType): void;
}
declare const RadioButtons: FC<RadioButtonsProps>;
export default RadioButtons;
