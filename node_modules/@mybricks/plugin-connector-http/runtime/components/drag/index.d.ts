import { FC, ReactNode } from 'react';
interface DragProps {
    children: ReactNode;
    item: any;
    parent?: any;
    draggable?: boolean;
    onDrop(dragItem: any, dropItem: any, place: 'bottom' | 'top' | 'inner'): void;
}
declare const Drag: FC<DragProps>;
export default Drag;
