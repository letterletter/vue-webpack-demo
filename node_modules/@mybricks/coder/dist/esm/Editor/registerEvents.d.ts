type EditorEvent = {
    name: string;
    callback: (e: Event) => void;
};
export type Handle = {
    dispose(): void;
};
export declare const registerEvents: (editor: editor, events: Array<EditorEvent>, handles: Array<Handle>) => Handle[];
export {};
