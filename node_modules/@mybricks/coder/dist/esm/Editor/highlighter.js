import { MonacoJsxSyntaxHighlight, getWorker } from "monaco-jsx-syntax-highlight";
export var setJsxHighlight = function setJsxHighlight(editor, monaco) {
  var monacoJsxSyntaxHighlight = new MonacoJsxSyntaxHighlight(getWorker(), monaco);
  var _monacoJsxSyntaxHighl = monacoJsxSyntaxHighlight.highlighterBuilder({
      editor: editor
    }),
    highlighter = _monacoJsxSyntaxHighl.highlighter,
    dispose = _monacoJsxSyntaxHighl.dispose;
  highlighter();
  editor.onDidChangeModelContent(function () {
    highlighter();
  });
  return dispose;
};